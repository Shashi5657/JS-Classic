alert("Hello");
//order of operations
// multiply & divide first - but these both have same priority
// add & subtract next - these both have same priority
// this is called as operator precedence
// we can use brackets to control this
// ex: (1+1) * 3

//rounding a decimal number
console.log(Math.round(3.55));
